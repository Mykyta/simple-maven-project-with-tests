name: TestHello

on: [push]

jobs:
  file_presence:
    runs-on: ubuntu-latest
    steps:
      - name: Tell your name
        uses: Mykyta/hello-world-javascript-action@3.0.1
        with:
          name: "Nick"
